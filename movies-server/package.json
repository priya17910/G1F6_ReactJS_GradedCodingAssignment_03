{
  "name": "movie-server",
  "version": "1.0.0",
  "description": "Movie On The Tip Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch data.json --port 4000",
    "start:delay": "npm start -- --delay 2000"
  },
  "author": "Priya Gupta",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}